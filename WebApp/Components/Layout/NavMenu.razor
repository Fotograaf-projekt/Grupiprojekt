@implements IDisposable
@inject NavigationManager Nav

<nav class="site-navbar">
    <a class="navbar-brand" href="/">
        FOTO<br />GRAAF
    </a>

    <button class="navbar-menu-btn" @onclick="ToggleMenu">
        MENU
        <div class="hamburger">
            <span></span><span></span>
        </div>
    </button>
</nav>

<div class="nav-overlay @(menuOpen ? "open" : "")">
    <button class="nav-overlay-close" @onclick="ToggleMenu">✕ SULGE</button>
    <ul class="nav-overlay-links">
        <li><a href="/" @onclick="ToggleMenu">Avaleht</a></li>
        <li><a href="/about" @onclick="ToggleMenu">Minust</a></li>
        <li><a href="/services" @onclick="ToggleMenu">Teenused</a></li>
        <AuthorizeView>
            <Authorized>
                <li><a href="/admin/photographer" @onclick="ToggleMenu">Admin · Profiil</a></li>
                <li><a href="/admin/services" @onclick="ToggleMenu">Admin · Teenused</a></li>
                <li><a href="/admin/social-links" @onclick="ToggleMenu">Admin · Sotsiaalmeedia</a></li>
                <li>
                    <form action="Account/Logout" method="post">
                        <AntiforgeryToken />
                        <input type="hidden" name="ReturnUrl" value="@currentUrl" />
                        <button type="submit"
                                style="background:none;border:none;padding:0;font-family:inherit;font-size:2.5rem;font-weight:300;color:var(--dark);cursor:pointer;letter-spacing:0.05em;">
                            Logi välja
                        </button>
                    </form>
                </li>
            </Authorized>
            <NotAuthorized>
                <li><a href="Account/Login" @onclick="ToggleMenu">Logi sisse</a></li>
            </NotAuthorized>
        </AuthorizeView>
    </ul>
</div>

@code {
    private bool menuOpen;
    private string? currentUrl;

    protected override void OnInitialized()
    {
        currentUrl = Nav.ToBaseRelativePath(Nav.Uri);
        Nav.LocationChanged += OnLocationChanged;
    }

    private void ToggleMenu() => menuOpen = !menuOpen;

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        menuOpen = false;
        currentUrl = Nav.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose() => Nav.LocationChanged -= OnLocationChanged;
}
